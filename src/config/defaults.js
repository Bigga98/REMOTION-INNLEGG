// ============================================
// DEFAULTS — Standardverdier for alle templates
// ============================================
// Overstyres av content JSON per innlegg.

export const defaults = {
  // Visuelt tema
  theme: "gray",
  gradient: "coral",

  // Felles elementer
  showLogo: true,
  showLeaf: true,
  showOverlay: true,
  leafOpacity: 0.12,

  // Tekst
  textAlign: "center",

  // Format & video
  format: "post",
  videoPreset: "standard",
  hookTiming: "standard",
};
